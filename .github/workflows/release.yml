name: New release was published

on:
  release:
    types: [published]

jobs: 
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Build
      run: |
        ls -a
        echo "tag ${{ github.ref_name }}"